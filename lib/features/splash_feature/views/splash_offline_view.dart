import 'package:flutter/material.dart';

class SplashOfflineView extends StatelessWidget {
  const SplashOfflineView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
