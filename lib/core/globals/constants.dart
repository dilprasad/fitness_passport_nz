enum EButtonType {
  bText,
  bIcon,
  bIconText,
}

enum EButtonState {
  bActive,
  bDisable,
  bLoading,
}

enum EInternetStatus {
  iOffline,
  iOnline,
  iLoading,
}

enum EUserStatus {
  uNotLoggedIn,
  uLoggedIn,
  uAuthenticated,
  uLoggedOut,
  // uRegistered,
  // uNotRegistered,
}

enum EApiStatus {
  aLoading,
  aCompleted,
  aError,
}
